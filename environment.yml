name: dq-workbench
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  # conda handles the scientific stack (binary wheels, faster solves)
  - numpy>=1.26,<2.0
  - scipy>=1.12
  - pandas>=2.0,<2.3
  - pip
  - pip:
    - -e ".[dev]"
